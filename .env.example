# Frontend API Base URL (Leave blank for relative proxy in dev)
VITE_API_BASE_URL=

# Backend Configuration
PORT=8000
HOST=0.0.0.0

# Optional LLM API Key (Google Gemini / OpenAI)
# If left blank, SkyAssist runs deterministically on grounded policy engine
LLM_API_KEY=
LLM_MODEL=gemini-1.5-flash
